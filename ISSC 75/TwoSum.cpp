class Solution {
    public:
        vector<int> twoSum(vector<int>& nums, int target) 
        {
            //Logic, LLO: if the two numbers give an expected result, mark their index and return that o/p. Two parts: Traversal and checking  
        }
    };